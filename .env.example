# SL接近時のAIニュースチェック機能（position_monitor.py）
# 未設定の場合、この機能は自動的に無効化される（既存のTP/SL監視のみ動作）。
GOOGLE_API_KEY=

# フェーズA(デフォルト・false): AI判定をai_sl_checksテーブルへ記録するのみ。
# フェーズB(true): STRUCTURAL判定時に実際に早期売りを実行する。
AI_SL_CHECK_ACTIVE=false
